query listBannedUsers($page: Int, $limit: Int) {
  listBannedUsers(page: $page, limit: $limit) {
    persons {
      person {
        id
        name
        displayName
        avatar
        isAdmin
        isBanned
        creationDate
        unbanDate
      }
    }
    totalCount
  }
}
