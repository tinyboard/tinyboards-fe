enum ListingType {
  all
  subscribed
  local
  moderated
}

enum SortType {
  active,
  hot,
  new,
  old,
  topDay,
  topWeek,
  topMonth,
  topYear,
  topAll,
  mostComments,
  newComments
}


query listPosts(
  $listingType: ListingType!
  $sort: SortType
  $page: Int
  $limit: Int
  $boardId: Int
  $userId: Int
  $personName: String
  $boardName: String
  $savedOnly: Boolean
  $includeBoard: Boolean!
) {
  listPosts(
    listingType: $listingType
    sort: $sort
    page: $page
    limit: $limit
    boardId: $boardId
    userId: $userId
    personName: $personName
    boardName: $boardName
    savedOnly: $savedOnly
  ) {
    id
    titleChunk
    title
    url
    bodyHTML
    isRemoved
    isDeleted
    isLocked
    isNSFW
    featuredBoard
    featuredLocal
    score
    myVote
    commentCount
    creationDate
    updated
    myModPermissions
    altText
    embedTitle
    embedDescription
    embedVideoUrl
    sourceUrl
    lastCrawlDate
    creator {
      id
      name
      instance
      adminLevel
      displayName
      avatar
    }
    board @include(if: $includeBoard) {
      id
      name
      title
      icon
      primaryColor
    }
  }
}
