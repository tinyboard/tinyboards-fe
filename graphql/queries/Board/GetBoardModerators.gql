query getBoardModerators($boardId: Int!) {
  getBoardModerators(boardId: $boardId) {
    id
    boardId
    user {
      name
      displayName
      avatar
      isAdmin
    }
    creationDate
    permissions
    rank
    inviteAccepted
    inviteAcceptedDate
  }
}
