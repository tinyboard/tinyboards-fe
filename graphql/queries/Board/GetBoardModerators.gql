query getBoardModerators($boardId: Int!) {
  getBoardModerators(boardId: $boardId) {
    id
    boardId
    person {
      name
      displayName
      avatar
      isAdmin
    }
    creationDate
    permissions
    rank
    inviteAccepted
    inviteAcceptedDate
  }
}
