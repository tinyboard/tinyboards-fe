mutation reportContent($contentType: String!, $contentId: Int!, $reason: String!) {
  reportContent(contentType: $contentType, contentId: $contentId, reason: $reason) {
    id
    reason
    resolved
    created
  }
}