mutation useEmoji($emojiId: Int!) {
  useEmoji(emojiId: $emojiId) {
    id
    shortcode
    imageUrl
    altText
    category
    creationDate
    updated
    boardId
    createdByUserId
    isActive
    usageCount
    emojiScope
  }
}