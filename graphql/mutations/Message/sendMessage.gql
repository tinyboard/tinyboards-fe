mutation sendMessage($input: SendMessageInput!) {
  sendMessage(input: $input) {
    message {
      id
      title
      body
      bodyHTML
      published
      updated
      creator {
        id
        name
        displayName
        avatar
      }
      recipient {
        id
        name
        displayName
        avatar
      }
    }
  }
}