mutation editMessage($input: EditMessageInput!) {
  editMessage(input: $input) {
    message {
      id
      title
      body
      published
      updated
      creator {
        id
        name
        displayName
        avatar
      }
      recipient {
        id
        name
        displayName
        avatar
      }
    }
  }
}